name: Docker Image CI

on:
  push:
    branches: [ "main", "Romain_Leveillé-Nizerolle" ]
  pull_request:
    branches: [ "main", "Romain_Leveillé-Nizerolle" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
